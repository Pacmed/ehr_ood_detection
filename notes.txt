# RUN JUPYTER ON A SERVER

remote machine:
    cd ../..
    jupyter notebook --no-browser --port=8888

local machine:
    ssh -N -f -L localhost:8888:localhost:8888 karina@10.60.0.26

go to: http://localhost:8888
enter toekn from remote machine


# APPEND PATH
export PYTHONPATH=$PYTHONPATH:/home/karina/ehr_ood_detection/src


# RUN:
    python3 src/experiments/out_of_domain.py --data-origin eICU --models LOF

    python3 src/experiments/plot_results.py --data-origin MIMIC -p ood  --plot-type heatmap  --show-percentage-sig  --show-rel-sizes --result-dir  data/hyperparameters/ --plot-dir data/hyperparameters/ --models

    python3 src/experiments/plot_results.py --data-origin MIMIC -p ood  --plot-type heatmap  --show-percentage-sig  --show-rel-sizes

    python3 src/experiments/plot_results.py --data-origin MIMIC -p pertrub  --plot-type heatmap



# SWITCHES

    utils/datahandler.py > line 73
    models/info.py > line (top)